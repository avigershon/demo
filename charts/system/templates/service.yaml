apiVersion: v1
kind: Service
metadata:
  name: {{ template "system.fullname" . }}
  labels:
    app: {{ template "system.name" . }}
    chart: {{ template "system.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "system.name" . }}
    release: {{ .Release.Name }}
