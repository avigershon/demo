apiVersion: apps/v1beta2
kind: Service
metadata:
  creationTimestamp: null
  labels:
    name: {{ template "blackbird.name" . }}
  name: {{ template "blackbird.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - name: "{{ .Values.service.port }}"
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
  selector:
    name: {{ template "blackbird.name" . }}
